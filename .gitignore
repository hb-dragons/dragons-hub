node_modules/
.pnpm-store/
.turbo/
dist/
coverage/
apps/*/coverage/

.DS_Store

.env
.env.*
!.env.example

apps/*/.next/
apps/*/dist/
apps/*/tsconfig.tsbuildinfo

# Worktrees
.worktrees/
.claude/worktrees/

# Superpowers brainstorm sessions
.superpowers/

# next-intl generated type declarations
apps/web/src/messages/*.d.json.ts

# Extracted test seed contains real federation PII — regenerate via
# `pnpm seed:extract`, never commit it (GDPR; no consumer needs it in git)
apps/api/src/test/fixtures/seed.json

# Python bytecode
**/__pycache__/
*.pyc

# Protocol reverse-engineering scratch captures (not committed; promote
# curated fixtures to apps/api/src/services/scoreboard/__fixtures__/ instead)
apps/pi/research/

# Real wifi keys for the roaming Pi (mode 0600 on the box; see networks.conf.example)
apps/pi/networks.conf

# Local database dumps (never commit; contains federation PII)
backups/

# Astro generated types (regenerated by astro sync/check/build)
apps/*/.astro/
