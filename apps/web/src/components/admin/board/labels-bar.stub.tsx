"use client";

export interface LabelsBarProps {
  labels?: { id: number; color: string; name?: string | null }[];
}

/** Stub: renders nothing until backend labels resource ships.
 *  Enablement: swap this file's default export for a real <LabelsBar>. */
export function LabelsBar(_props: LabelsBarProps) {
  return null;
}
